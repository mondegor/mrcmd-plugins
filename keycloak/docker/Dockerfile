ARG DOCKER_IMAGE_FROM

FROM ${DOCKER_IMAGE_FROM}

ARG DB_TYPE
ARG DB_USER
ARG DB_PASSWORD
ARG DB_URL

ENV KC_DB=${DB_TYPE}
ENV KC_DB_USERNAME=${DB_USER}
ENV KC_DB_PASSWORD=${DB_PASSWORD}
ENV KC_DB_URL=${DB_URL}

WORKDIR /opt/keycloak

# COPY *.jar ./providers
# RUN ./bin/kc.sh build

# COPY keycloak.conf /opt/keykloack/conf/keycloak.conf

#EXPOSE 8080
#EXPOSE 8443